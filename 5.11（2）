#include <stdio.h>
int main(){
	int ch=0;
	while((ch=getchar())!=EOF){//!表示非，非前面的整体要加括号
	//遇见非EOF意思是该程序持续进行，只有按Ctrl+z时，才可以使程序结束。 
	if(ch<'0'||ch>'9'){ //意思是如果输入的字符小于0或者>9,那么是不输出的，就是只能输出数字字符 
	continue;
	}
	else{
	putchar(ch);
	}
	}
	}//满足条件直接跳过本次循环。进入循环判断条件看是否满足。 
